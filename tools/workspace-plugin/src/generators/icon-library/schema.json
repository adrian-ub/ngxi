{
  "$schema": "https://json-schema.org/schema",
  "$id": "IconLibrary",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "prefix": {
      "type": "string",
      "description": "The prefix to use for the icon library.",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": {
        "message": "What prefix would you like to use?"
      }
    },
    "iconsSourcePath": {
      "type": "string",
      "description": "The path to the icons source directory.",
      "$default": {
        "$source": "argv",
        "index": 2
      },
      "x-prompt": {
        "message": "What is the path to the icons source directory?"
      }
    }
  },
  "required": ["name", "prefix", "iconsSourcePath"]
}
